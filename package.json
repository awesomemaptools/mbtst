{
  "name": "mbtst",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "node node_modules/http-server/bin/http-server -c-1 -a localhost",
    "build": "node node_modules/browserify/bin/cmd.js --entry src/main.js --outfile dist/build.js" 
  },
  "author": "ellvtr",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^14.1.0",
    "http-server": "^0.9.0",
    "mapbox-gl": "^0.34.0"
  }
}
